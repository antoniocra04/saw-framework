---
description: Create a spec with testable Acceptance Criteria for a new task (BSA role)
agent: bsa
---
Create a spec for the following request, following your BSA procedure exactly
(next TASK id, mandatory pattern discovery, fill every section of the template,
3–7 testable acceptance criteria, explicit Out of scope):

REQUEST: $ARGUMENTS

Current specs directory:
!`ls .workflow/specs 2>/dev/null || echo "(empty)"`
